name: Validate skills

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependencies
        run: python -m pip install "PyYAML>=6,<7" "requests>=2,<3"

      - name: Validate skills and Python syntax
        run: python scripts/validate_repo.py

      - name: Check generated skill catalog
        run: python scripts/generate_catalog.py --check

      - name: Test skill catalog
        run: python -m unittest discover -s tests -p "test_*.py" -v

      - name: Test safe directory submission
        run: python -m unittest discover -s seo-backlink-submitter/tests -p "test_*.py" -v

      - name: Test code-page generator and examples
        run: python -m unittest discover -s multi-game-codes-hub/tests -p "test_*.py" -v

      - name: Test Roblox site pipeline contracts
        run: python -m unittest discover -s roblox-site-architect/tests -p "test_*.py" -v

      - name: Test Roblox hit evaluator
        run: python -m unittest discover -s roblox-hit-evaluator/tests -p "test_*.py" -v

      - name: Test SignalLayer client
        run: python -m unittest discover -s signallayer-backlinks-client/tests -p "test_*.py" -v
